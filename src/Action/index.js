import * as auth from './RegisterAction'
import * as Login from './Login'
import * as get_category from './Category'
const authActions=({
    auth,
    Login,
    get_category
});

export default authActions;
